@using DraftView.Domain.Notifications
@model DraftView.Web.Models.SettingsViewModel
@{
    ViewData["Title"] = "Account Settings";
}

<div class="site-hero settings-hero">
    <div class="site-hero__content">
        <div class="site-hero__title">@(string.IsNullOrWhiteSpace(Model.DisplayName) ? "Account Settings" : Model.DisplayName)</div>
        <div class="site-hero__subtitle">Account Settings</div>
    </div>
</div>

<div class="settings-page">
    <div class="settings-layout">
        <div class="settings-layout__main">
            <div class="card settings-card">
                <div class="card__header"><span class="card__title">Profile</span></div>
                <div class="card__body">
                    <form asp-action="ChangeDisplayName" method="post">
                        @Html.AntiForgeryToken()
                        <div class="settings-form-row">
                            <div class="settings-form-field">
                                <label class="form-label">Display name</label>
                                <input type="text" name="DisplayName" value="@Model.DisplayName" class="form-input" required />
                            </div>
                            <button type="submit" class="btn btn--primary settings-form-button">Save</button>
                        </div>
                    </form>
                </div>
            </div>

            <div class="card settings-card">
                <div class="card__header"><span class="card__title">Appearance</span></div>
                <div class="card__body">
                    <form asp-action="ChangeDisplayTheme" method="post">
                        @Html.AntiForgeryToken()
                        <div class="settings-form-row">
                            <div class="settings-form-field">
                                <label class="form-label">Display theme</label>
                                <select name="DisplayTheme" class="form-input" required>
                                    <optgroup label="Adventure">
                                        <option value="Light"             selected="@(Model.DisplayTheme == "Light")">Adventure — Light</option>
                                        <option value="Dark"              selected="@(Model.DisplayTheme == "Dark")">Adventure — Dark</option>
                                    </optgroup>
                                    <optgroup label="Crime / Glasgow Rain">
                                        <option value="CrimeLight"        selected="@(Model.DisplayTheme == "CrimeLight")">Crime — Light</option>
                                        <option value="CrimeDark"         selected="@(Model.DisplayTheme == "CrimeDark")">Crime — Dark</option>
                                    </optgroup>
                                    <optgroup label="Noir / Cold War Spy">
                                        <option value="NoirLight"         selected="@(Model.DisplayTheme == "NoirLight")">Noir — Light</option>
                                        <option value="NoirDark"          selected="@(Model.DisplayTheme == "NoirDark")">Noir — Dark</option>
                                    </optgroup>
                                    <optgroup label="Contemporary">
                                        <option value="ContemporaryLight" selected="@(Model.DisplayTheme == "ContemporaryLight")">Contemporary — Light</option>
                                        <option value="ContemporaryDark"  selected="@(Model.DisplayTheme == "ContemporaryDark")">Contemporary — Dark</option>
                                    </optgroup>
                                    <optgroup label="Historic / Georgian">
                                        <option value="HistoricLight"     selected="@(Model.DisplayTheme == "HistoricLight")">Historic — Light</option>
                                        <option value="HistoricDark"      selected="@(Model.DisplayTheme == "HistoricDark")">Historic — Dark</option>
                                    </optgroup>
                                </select>
                            </div>
                            <button type="submit" class="btn btn--primary settings-form-button">Save</button>
                        </div>
                    </form>
                </div>
            </div>

            @if (Model.IsAuthor || Model.IsReader)
            {
                <div class="card settings-card">
                    <div class="card__header"><span class="card__title">Reading Preferences</span></div>
                    <div class="card__body">
                        <form asp-action="ChangeProseFontPreferences" method="post">
                            @Html.AntiForgeryToken()
                            <div class="settings-form-row settings-form-row--two-fields">
                                <div class="settings-form-field">
                                    <label class="form-label">Font face</label>
                                    <select name="ProseFont" class="form-input" required>
                                        <option value="SystemSerif" selected="@(Model.ProseFont == "SystemSerif")">System Serif</option>
                                        <option value="Humanist" selected="@(Model.ProseFont == "Humanist")">Humanist</option>
                                        <option value="Classic" selected="@(Model.ProseFont == "Classic")">Classic</option>
                                        <option value="SansSerif" selected="@(Model.ProseFont == "SansSerif")">Sans-serif</option>
                                    </select>
                                </div>
                                <div class="settings-form-field">
                                    <label class="form-label">Font size</label>
                                    <select name="ProseFontSize" class="form-input" required>
                                        <option value="Small" selected="@(Model.ProseFontSize == "Small")">Small</option>
                                        <option value="Medium" selected="@(Model.ProseFontSize == "Medium")">Medium</option>
                                        <option value="Large" selected="@(Model.ProseFontSize == "Large")">Large</option>
                                        <option value="ExtraLarge" selected="@(Model.ProseFontSize == "ExtraLarge")">Extra Large</option>
                                    </select>
                                </div>
                                <button type="submit" class="btn btn--primary settings-form-button">Save</button>
                            </div>
                        </form>
                    </div>
                </div>
            }

            @if (Model.IsReader)
            {
                <div class="card settings-card reader-profile-card">
                    <div class="card__header"><span class="card__title">Reader Profile</span></div>
                    <div class="card__body">
                        <p class="settings-hint">This information helps authors understand who is requesting access to their books. All fields are optional.</p>
                        <form asp-action="UpdateReaderProfile" method="post">
                            @Html.AntiForgeryToken()
                            <div class="settings-form-stack">
                                <div class="settings-form-group">
                                    <label class="form-label">About me</label>
                                    <textarea name="ReaderBio" class="form-input" rows="3" maxlength="1000" placeholder="A short bio for authors to see...">@Model.ReaderBio</textarea>
                                </div>
                                <div class="settings-form-group">
                                    <label class="form-label">Genre interests</label>
                                    <input type="text" name="ReaderGenreInterests" class="form-input" value="@Model.ReaderGenreInterests" maxlength="500" placeholder="e.g. Fantasy, Sci-fi, Historical fiction" />
                                </div>
                                <div class="settings-form-group">
                                    <label class="form-label">Reading pace</label>
                                    <select name="ReaderPace" class="form-input">
                                        <option value="" selected="@(Model.ReaderPace == null)">Not specified</option>
                                        <option value="Slow" selected="@(Model.ReaderPace == "Slow")">Slow</option>
                                        <option value="Steady" selected="@(Model.ReaderPace == "Steady")">Steady</option>
                                        <option value="Fast" selected="@(Model.ReaderPace == "Fast")">Fast</option>
                                    </select>
                                </div>
                                <button type="submit" class="btn btn--primary settings-form-button-inline">Save Profile</button>
                            </div>
                        </form>
                    </div>
                </div>
            }
        </div>

        <div class="settings-layout__side">
            <div class="card settings-card">
                <div class="card__header"><span class="card__title">Email Address</span></div>
                <div class="card__body">
                    <form asp-action="ChangeEmail" method="post">
                        @Html.AntiForgeryToken()
                        <div class="settings-form-stack">
                            <div class="settings-form-group">
                                <label class="form-label">New email address</label>
                                <input type="email" name="Email" value="@Model.Email" class="form-input" required />
                            </div>
                            <div class="settings-form-group">
                                <label class="form-label">Current password (to confirm)</label>
                                <input type="password" name="CurrentPassword" class="form-input" required />
                            </div>
                            <button type="submit" class="btn btn--primary settings-form-button-inline">Update Email</button>
                        </div>
                    </form>
                </div>
            </div>

            <div class="card settings-card">
                <div class="card__header"><span class="card__title">Change Password</span></div>
                <div class="card__body">
                    <form asp-action="ChangePassword" method="post">
                        @Html.AntiForgeryToken()
                        <div class="settings-form-stack">
                            <div class="settings-form-group">
                                <label class="form-label">Current password</label>
                                <input type="password" name="CurrentPassword" class="form-input" required />
                            </div>
                            <div class="settings-form-group">
                                <label class="form-label">New password</label>
                                <input type="password" name="NewPassword" class="form-input" required />
                            </div>
                            <div class="settings-form-group">
                                <label class="form-label">Confirm new password</label>
                                <input type="password" name="ConfirmPassword" class="form-input" required />
                            </div>
                            <button type="submit" class="btn btn--primary settings-form-button-inline">Change Password</button>
                        </div>
                    </form>
                </div>
            </div>

            @if (Model.IsAuthor)
            {
                <div class="card settings-card">
                    <div class="card__header"><span class="card__title">Dropbox</span></div>
                    <div class="card__body">
                        @if (Model.DropboxStatus == "Connected")
                        {
                            <p class="settings-status">
                                <span class="badge badge--success">Connected</span>
                                @if (Model.DropboxAuthorisedAt.HasValue)
                                {
                                    <span class="settings-status-meta">
                                        since @Model.DropboxAuthorisedAt.Value.ToString("dd MMM yyyy")
                                    </span>
                                }
                            </p>
                            <div class="settings-inline-actions">
                                <a asp-controller="Dropbox" asp-action="Connect" class="btn btn--secondary">Reconnect</a>
                                <form asp-controller="Dropbox" asp-action="Disconnect" method="post" class="settings-inline-form">
                                    @Html.AntiForgeryToken()
                                    <button type="submit" class="btn btn--danger">Disconnect</button>
                                </form>
                            </div>
                        }
                        else
                        {
                            <p class="settings-status settings-status--muted">
                                <span class="badge badge--neutral">Not connected</span>
                            </p>
                            <a asp-controller="Dropbox" asp-action="Connect" class="btn btn--primary">Connect Dropbox</a>
                        }
                    </div>
                </div>
            }
        </div>

        @if (Model.IsAuthor)
        {
            var activeLabel   = SettingsFilterLabel(Model.ActiveTypeFilter);
            var allClass      = SettingsChipClass(Model.ActiveTypeFilter, null);
            var commentClass  = SettingsChipClass(Model.ActiveTypeFilter, NotificationEventType.NewComment);
            var replyClass    = SettingsChipClass(Model.ActiveTypeFilter, NotificationEventType.ReplyToAuthor);
            var readerClass   = SettingsChipClass(Model.ActiveTypeFilter, NotificationEventType.ReaderJoined);
            var syncClass     = SettingsChipClass(Model.ActiveTypeFilter, NotificationEventType.SyncCompleted);
            <div class="card settings-card">
                <div class="card__header"><span class="card__title">Activity Log</span></div>

                <div class="notification-filters">
                    <a asp-action="Settings" class="@allClass">All</a>
                    <a asp-action="Settings" asp-route-type="@NotificationEventType.NewComment"    class="@commentClass">Comments</a>
                    <a asp-action="Settings" asp-route-type="@NotificationEventType.ReplyToAuthor" class="@replyClass">Replies</a>
                    <a asp-action="Settings" asp-route-type="@NotificationEventType.ReaderJoined"  class="@readerClass">Readers</a>
                    <a asp-action="Settings" asp-route-type="@NotificationEventType.SyncCompleted" class="@syncClass">Sync</a>
                </div>

                @if (Model.Notifications.Any())
                {
                    <ul class="notifications-list notifications-list--settings">
                        @foreach (var n in Model.Notifications)
                        {
                            <li class="notification-item">
                                @await Html.PartialAsync("_NotificationItem", n)
                                <form asp-controller="Author" asp-action="DismissNotification" method="post" style="display:contents">
                                    @Html.AntiForgeryToken()
                                    <input type="hidden" name="notificationId" value="@n.Id" />
                                    <button type="submit" class="notification-item__dismiss" aria-label="Dismiss">&#x2715;</button>
                                </form>
                            </li>
                        }
                    </ul>

                    <div class="settings-activity-actions">
                        @if (Model.ActiveTypeFilter.HasValue)
                        {
                            <form asp-action="ClearNotifications" method="post" style="display:inline">
                                @Html.AntiForgeryToken()
                                <input type="hidden" name="type" value="@Model.ActiveTypeFilter" />
                                <button type="submit" class="btn btn--ghost btn--sm">Clear @activeLabel</button>
                            </form>
                        }
                        else
                        {
                            <form asp-action="ClearNotifications" method="post" style="display:inline"
                                  onsubmit="return confirm('Clear all activity? This cannot be undone.')">
                                @Html.AntiForgeryToken()
                                <button type="submit" class="btn btn--ghost btn--sm">Clear All</button>
                            </form>
                        }
                    </div>
                }
                else
                {
                    <p class="settings-hint" style="margin-top:var(--space-4);">No activity to show.</p>
                }
            </div>
        }

    </div>
</div>

@functions {
    static string SettingsChipClass(NotificationEventType? active, NotificationEventType? chip) =>
        "notification-filter" + (active == chip ? " notification-filter--active" : "");

    static string? SettingsFilterLabel(NotificationEventType? type) => type switch
    {
        NotificationEventType.NewComment    => "Comments",
        NotificationEventType.ReplyToAuthor => "Replies",
        NotificationEventType.ReaderJoined  => "Readers",
        NotificationEventType.SyncCompleted => "Sync",
        _                                   => null
    };
}