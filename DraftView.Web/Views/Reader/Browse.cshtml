@model SectionContentsViewModel
@{ ViewData["Title"] = Model.TopLevelSection.Title; }

<p class="section-breadcrumb">
    <a asp-action="Index">@Model.ProjectName</a>
    <span class="section-breadcrumb__sep">&rsaquo;</span>
</p>
<h2 class="section-title">@Model.TopLevelSection.Title</h2>

@await Html.PartialAsync("_ContentGroups", Model.Groups)
