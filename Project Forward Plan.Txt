Minor Fixes
===========
Author Chapter Level Page needed, as well as Scene level page.

Chapter page should see scene comments on RHS and Chapter level comments on floating bar at the bottom. Scene level page should have a link to chapter page. Scene page "2 readers" should have a hover over that displays which readers.

New requirement. Floating bar at very bottom that retains Copyright details and has a System status - System Outages etc. Also a Report Fault button that will open a popup, or a full page - not sure which is best, that will then privately email support@draftreader.co.uk. I need help setting up email redirection of this to alastair_clarke@yahoo.com on CloudFlare website, if they provide it.

Also, Only allow Chapter Level publishing - Scene level publishing seems too detailed.  Part level publishing will automatically publish all chapters below, and by definition, all scenes below each chapter.  Book level publishing as well.

Major Features
==============
Publishing for Readers should have CSS for iOS, Android, phones, tablets and full screens.


Phase 1 Item 1 remaining defects
1. Reader display name is not persisted on invitation acceptance

Severity: high
Why: core functional bug
Observed: reader enters name, account is created, but Readers list still shows Pending instead of entered name.

Likely fix area:

UserService.AcceptInvitationAsync(...)
possibly domain User entity needs a way to set/update display name during acceptance
possibly controller flow if name is not passed through correctly to domain persistence

Fix first.

2. Invitation email does not include expiry information

Severity: medium
Why: behaviour is incomplete and misleading for expiring invites
Observed: expiring invite works, but email body gives only link, no expiry wording.

Needed behaviour:

never-expiring invite email says it does not expire, or says nothing time-bound
expiring invite email clearly states expiry date/time, ideally in readable local form

Likely fix area:

UserService.IssueInvitationAsync(...)

Fix second.

3. Reader list/status wording is misleading for pending invites

Severity: medium
Why: UI truthfulness issue
Observed: newly invited reader shows:

name = Pending
status = Inactive
joined = Pending

That is technically understandable, but poor wording.

Better state model for UI:

status should distinguish:
Invited
Active
Inactive
Pending should not be used as a fake display name in the main list

Likely fix area:

Readers view / view model
maybe invite-created users should not use "Pending" as stored display name, or UI should mask it

Fix third.

4. Invite email recipient name is generic

Severity: low
Why: polish / clarity
Observed: email goes to:

To: Invited Reader <test5@gmail.com>

That is acceptable in dev, but weak.

Possible improvement:

use email local part
or no name
or a better placeholder

Fix fourth.

Things that appear to be working

These are not current defects:

expiring invite can be created
expired invite is refused correctly
refusal page is clear enough for now
invite send path works
acceptance path creates reader account
console email in dev works
Correct fix order
Order of work
Persist accepted reader display name
Add expiry text to invite email
Correct Readers page status/display wording
Tidy email recipient display name
Why this order

Because:

item 1 is a real data integrity problem
item 2 is missing business behaviour
item 3 is UI truthfulness and clarity
item 4 is polish only
Suggested Stage split
Stage 1A

Persist accepted reader name correctly

Stage 1B

Add expiry wording to invitation emails

Stage 1C

Clean Readers screen state labels for invited vs active vs inactive

Stage 1D

Minor email presentation polish